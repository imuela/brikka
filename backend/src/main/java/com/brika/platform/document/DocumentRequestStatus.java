package com.brika.platform.document;

/** Conservative technical default for Sprint 4 (provisional, not a business catalog). */
public enum DocumentRequestStatus {
  PENDING,
  FULFILLED,
  CANCELLED
}
