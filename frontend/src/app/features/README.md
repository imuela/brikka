# features

One subfolder per business domain (e.g. `clients/`, `cases/`, `documents/`, `portal/`), each
package-by-feature: its own components, services, routes, state. No business logic in Sprint 1
— see `25_CLAUDE_CODE_EXECUTION_GUIDE.md` for which sprint owns each domain.
